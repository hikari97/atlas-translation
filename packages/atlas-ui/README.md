# Atlas UI

`@atlas/atlas-ui` provides framework-friendly UI contracts and reusable component descriptors for Atlas Studio.

The package contains component primitives, registry, theme tokens, context, accessibility checks, icon registry, overlay models, editor widgets, integrations, diagnostics, examples, and benchmarks.

It does not contain application state or business logic.
