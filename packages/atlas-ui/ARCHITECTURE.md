# Architecture

Atlas UI is the presentation contract layer for Atlas Studio.

Components are JSON-safe descriptors that can be adapted by applications to React, Chakra UI, or other host UI frameworks. Business logic stays outside this package.
