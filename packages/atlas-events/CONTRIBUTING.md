# Contributing

- Keep dependencies limited to `@atlas/atlas-types` and `@atlas/atlas-document`.
- Do not introduce UI, networking, command, core, app, or plugin dependencies.
- Run `npm run typecheck`, `npm run build`, and `npm run test:types`.
