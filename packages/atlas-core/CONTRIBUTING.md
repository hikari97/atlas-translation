# Contributing

- Keep dependencies within approved package graph.
- Do not add UI, rendering, app, plugin, or business workflow code.
- Run `npm run typecheck`, `npm run build`, and `npm run test:types`.
