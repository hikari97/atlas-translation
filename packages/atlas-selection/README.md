# Atlas Selection

`@atlas/atlas-selection` provides the centralized selection engine for Atlas Studio.

It supports selection state, sessions, strategies, ranges, marquee selection, hierarchy, navigation, accessibility metadata, history, snapshots, diagnostics, and metrics.
