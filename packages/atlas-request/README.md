# Atlas Request

Atlas Request provides provider-independent application request contracts for Atlas Studio.

It models `HttpRequest`, request body, headers, query, parameters, cookies, files, session, context, metadata, lifecycle, core services, and providers without depending on runtime request objects, networking, parsers, validation, authentication, or storage implementations.
