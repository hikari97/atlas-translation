# Atlas Controller

Atlas Controller provides provider-independent controller contracts and execution orchestration for Atlas Studio.

It models controller contracts, contexts, registries, resolvers, dispatchers, executors, metadata, lifecycle, builders, providers, engines, and factories without depending on web frameworks, networking, route matching, validation, dependency injection, or response serialization.
