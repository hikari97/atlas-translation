/**
 * Supported Atlas plugin categories.
 */
export enum PluginType {
  OCR = 'ocr',
  Translation = 'translation',
  Vision = 'vision',
  Inpainting = 'inpainting',
  Export = 'export',
  Import = 'import',
  Storage = 'storage',
  Automation = 'automation'
}
