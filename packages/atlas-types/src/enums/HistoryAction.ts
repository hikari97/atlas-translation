/**
 * History operation categories.
 */
export enum HistoryAction {
  Create = 'create',
  Update = 'update',
  Delete = 'delete',
  Move = 'move',
  Resize = 'resize',
  Restore = 'restore',
  Import = 'import',
  Export = 'export'
}
