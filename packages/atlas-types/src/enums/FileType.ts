/**
 * High-level file categories used by Atlas Studio.
 */
export enum FileType {
  Image = 'image',
  Font = 'font',
  Project = 'project',
  Archive = 'archive',
  Document = 'document',
  Unknown = 'unknown'
}
