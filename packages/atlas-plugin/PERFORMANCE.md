# Performance

Benchmarks cover registration, dependency sorting, lifecycle execution, discovery, and startup.

The baseline implementation uses ordered maps and deterministic topological sorting. All benchmark files are typechecked by `npm run test:types`.
