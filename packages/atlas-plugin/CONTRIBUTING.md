# Contributing

- Use TypeScript strict mode.
- Export public API through `src/index.ts`.
- Keep plugin runtime logic independent from renderer, UI, workspace, and apps.
- Use only public APIs from other Atlas packages.
