# Performance

The package exposes immutable data contracts and lightweight registry helpers only. Runtime hosts can measure transport, synchronization, and storage performance in their provider implementations.
