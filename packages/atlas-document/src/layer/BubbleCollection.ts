import { DocumentCollection } from '../collection';
import type { BubbleDocument } from '../bubble';

/**
 * Collection of bubbles owned by a layer.
 */
export class BubbleCollection extends DocumentCollection<BubbleDocument> {}
