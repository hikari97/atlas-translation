# Atlas Middleware

Atlas Middleware provides provider-independent middleware contracts and execution orchestration for Atlas Studio.

It models middleware contracts, contexts, immutable pipelines, chains, registries, resolvers, executors, metadata, lifecycle, builders, providers, and engines without depending on web frameworks, networking, routing runtimes, dependency injection containers, or transport-specific response objects.
