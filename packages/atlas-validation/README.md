# Atlas Validation

Atlas Validation provides provider-independent validation contracts and rule execution orchestration for Atlas Studio.

It models validation contracts, contexts, registries, resolvers, rules, executors, metadata, lifecycle, builders, providers, engines, and factories without depending on web frameworks, networking, schema libraries, request parsers, dependency injection, or response generation.
