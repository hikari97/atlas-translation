# Performance

EPIC-003 establishes benchmark scenarios for:

- Command dispatch
- Pipeline behavior execution
- Resolver lookup
- History recording
- Undo and redo stacks
- Composite commands
- Macro commands

The current release provides deterministic benchmark stubs to preserve scenario definitions before runtime benchmark tooling is introduced.
