# Release Certification

- TypeScript strict mode passes.
- Type contract tests pass.
- Package build passes.
- Smoke tests cover resolved and unresolved authentication.
- Package remains provider independent and does not ship web server, crypto provider, OAuth, OIDC, SAML, LDAP, or JWT implementation code.
